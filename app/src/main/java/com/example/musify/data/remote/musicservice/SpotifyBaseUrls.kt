package com.example.musify.data.remote.musicservice

/**
 * An object that contains the different base URL's used to create
 * an instance of [SpotifyService].
 */
object SpotifyBaseUrls {
    const val API_URL = "https://api.spotify.com/"
    const val AUTHENTICATION_URL = "https://accounts.spotify.com/"
}